<div>
  <div><p>Nombre: </p>{{user.first_name}}</div>
  <div><p>Apellido: </p>{{user.last_name}}</div>
  <div><p>Email: </p>{{user.email}}</div>
    {{#if isAdmin}}
            <p>Tu rol es administrador</p>
    {{else}}
        <button onclick="swapRole()">Cambia a {{#if isPremium}}usuario comun{{else}}usuario premium{{/if}}</button>
    {{/if}}
</div>